# Python_project
Developed a Student Management System using Python that stores and manages student records in an Excel spreadsheet using the openpyxl library. The application provides a menu-driven interface to perform CRUD (Create, Read, Update, Delete) operations on student data. 
